# Copy this file to .env and fill in values
PORT=5000

# MariaDB connection (same unicentapos database as the POS)
DB_HOST=mariadb
DB_PORT=3306
DB_NAME=unicentapos
DB_USER=unicenta
DB_PASSWORD=unicentapos
